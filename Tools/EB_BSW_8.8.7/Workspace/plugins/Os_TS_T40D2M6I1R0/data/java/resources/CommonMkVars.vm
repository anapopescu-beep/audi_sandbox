#* *### Some common variables to be used in the Microkernel templates are added below
#if( $OsModel.isMicroKernel() )
#* *##set( $MkVersion = $OsModel.getMkVersion() )
#* *##set( $IsMkVersionOlderThan21 = $ClassOfHelpers.isMkVersionOlderThan21($MkVersion) )
#* *##if( $IsMkVersionOlderThan21 )
#*    *##set( $MkCtrsubPrefix  = "QMOS" )
#*    *##set( $MkCsPrefix      = "QM" )
#*    *##set( $MkCfgPrefix     = "" )
#* *##else
#*    *##set( $MkCtrsubPrefix  = "CTRSUB" )
#*    *##set( $MkCsPrefix      = "CS" )
#*    *##set( $MkCfgPrefix     = "CFG_" )
#* *##end
#end